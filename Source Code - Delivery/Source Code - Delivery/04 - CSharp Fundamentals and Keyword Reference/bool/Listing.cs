using System;

class Listing {

    static void Main(string[] args) {

        // create a bool variable and assign true to it
        bool myVariable = true;

        // wait for input before exiting
        Console.WriteLine("Press enter to finish");
        Console.ReadLine();
    }
}